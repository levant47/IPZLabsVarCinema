import * as React from "react";

export const LogIn = () => (
    <main>
        <form>
            <div>
                <label>Email:</label>
                <input type="text" placeholder="email" />
            </div>

            <div>
                <label>Password:</label>
                <input type="password" />
            </div>
        </form>
    </main>
);
